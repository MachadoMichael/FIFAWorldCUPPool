import styled, { css } from "styled-components";

export const Container = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  margin-top: 1%;
  margin-left: 1%;

  &:hover {
    cursor: pointer;
  }
`;
